<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.rotlabs.flakerandroidretrofit">

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        tools:targetApi="33">
        <activity
            android:name="io.rotlabs.flakerandroidretrofit.FlakerActivity"
            android:exported="true"
            android:icon="@mipmap/ic_flaker"
            android:label="@string/companion_app_name"
            android:launchMode="singleInstance"
            android:roundIcon="@mipmap/ic_flaker"
            android:taskAffinity="io.rotlabs.flakerandroidretrofit.flaker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>
</manifest>